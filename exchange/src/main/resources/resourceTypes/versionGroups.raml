#%RAML 1.0 ResourceType
usage: Use this resourceType to get the version groups of an asset
description: Asset version groups

get:
  description: Gets asset version groups
  responses:
    200:
      body:
        application/json:
          example: !include ../examples/v2/version-groups.json
