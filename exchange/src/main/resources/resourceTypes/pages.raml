#%RAML 1.0 ResourceType

usage: Use this resourceType to represent a list of pages
description: A list of pages
get:
  description: Gets a list of pages
  is: [pagesOrderRevisionResponseHeader: { statusCode: 200 }]
  responses:
    200:
      body:
        application/json:
          example: !include ../examples/pages.json
          type: !include ../schemas/pages.json
