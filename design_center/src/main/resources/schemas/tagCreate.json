{
  "title": "Tag creation",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "maxLength": 64,
      "pattern": "^[a-zA-Z0-9_\\-\\.]*$"
    },
    "ref": {
      "description": "The tag name or commit SHA to create tag from",
      "type": "string",
      "maxLength": 64,
      "pattern": "^[a-zA-Z0-9_\\-\\.]*$"
    }
  },
  "required": ["name"]
}